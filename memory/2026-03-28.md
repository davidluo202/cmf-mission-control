# Memory — 2026-03-28

## Mission Control Dashboard (cmf-mission-control)

### Project
- Repo: `https://github.com/davidluo202/cmf-mission-control`
- Deployment: Railway (production URL: `https://cmf-mission-control-production.up.railway.app`)
- Structure: monorepo — `mission-control/server/` (Express + better-sqlite3) + `mission-control/client/` (React/Vite/TypeScript)
- Railway Root Directory: `mission-control`
- Dockerfile at `mission-control/Dockerfile` (build both server and client)

### Key Fixes Done Today

1. **better-sqlite3 ELF crash** — Mac arm64 `node_modules` was committed to git, Railway used Mac binary on Linux x86_64 → crash. Fixed by: removing `server/node_modules/` from git tracking, adding `server/.gitignore`, Dockerfile runs `npm ci` on Linux to recompile natively.

2. **Frontend never updated** — `server/client-dist/` was pre-built on Mac and committed. Fixed by: updating Dockerfile to build client in Railway (`cd client && npm ci && npm run build && cp -a /app/client/dist/. /app/server/client-dist/`) so every deploy rebuilds the frontend.

3. **Login password bug** — wrong password showed infinite "Verifying..." spinner instead of error message. Fixed Login.tsx with proper error handling and 5-attempt lockout. However password auth still broken (unknown reason — both `cmf-mc-2026` and custom Railway env var failed).

4. **Auth disabled (temp)** — David asked to bypass login entirely until dashboard is stable. Changes:
   - `server/index.js`: `/api/auth/login` always returns `{ ok: true, token: API_TOKEN }`
   - `client/src/App.tsx`: removed login gate, removed `import Login` (caused TS build error, fixed in follow-up commit)

### Current State (end of day)
- Auth: **disabled** (bypass mode)
- Last commits: `742a8b4` (fix unused Login import), `2c1961e` (disable auth), `dc362c4` (Dockerfile client build), `218e5d2` (remove node_modules from git)
- Railway should be deploying `742a8b4` — dashboard should be accessible without login
- TODO: re-enable auth once dashboard is confirmed working, investigate why password comparison failed (possible encoding/whitespace issue in env var)

### API Details
- `API_TOKEN`: `cmf-mc-token-2026` (used by client as `x-api-token` header)
- `DASHBOARD_PASSWORD`: env var in Railway (default `cmf-mc-2026`) — currently bypassed
- `DATA_DIR`: `/data` (Railway persistent volume)
- Port: `8765` (Railway maps to public URL)

### Architecture Note
- Client `api.ts` calls `${protocol}//${hostname}:8765/api` — this works on Railway because Railway proxies port 8765. No baseURL hardcoding needed.
