# 2026-03-19

## CMFinancial 官网 AWS 安全合规（SFC）排查与加固
- SFC 合规方向：HTTPS/TLS、DDoS 防护、WAF（OWASP Top 10/SQLi/XSS）、审计日志、补丁与EOL软件管理、事件响应等。
- 通过账单与控制台核实：
  - AWS WAF 已存在并产生小额费用（$0.89），但需确认/完善关联与规则。
  - CloudFront 在升级前账单中未出现，需新建分配并将流量置于 CloudFront 前。
  - ACM 证书已签发（`*.cmfinancial.com` + `cmfinancial.com`），位于 `us-east-1`，但最初显示“使用中：否”（未绑定任何资源）。
- CloudFront 新建分配关键配置（用于动态站点/后台系统）：
  - Viewer protocol：Redirect HTTP→HTTPS。
  - Allowed methods：包含 POST/PUT/PATCH/DELETE。
  - Cache policy：建议 `CachingDisabled`（避免 query string 维度内容串号）。
  - Origin request policy：`AllViewer`（透传 Cookie/Header/Query，保证登录/鉴权）。
  - WAF：开启（非 monitor mode），Rate limiting 打开。
- 域名绑定问题：
  - `cmfinancial.com` 已成功作为 Alternate Domain Name 绑定到新 CloudFront 分配，并在 Cloudflare 侧创建 apex CNAME（使用 Cloudflare 的 CNAME flattening，且保持 DNS only 灰云避免“双层 CDN”）。
  - `www.cmfinancial.com` 无法绑定，CloudFront 报 CNAME 已被其他资源占用。
  - 通过 CloudShell `list-conflicting-aliases` 查到 `www.cmfinancial.com` 被**其他 AWS 账号**占用（与当前账号 `971422711834` 不同；冲突结果返回的 AccountId 尾号 `171994`），需找到该账号/第三方服务释放别名。

## Cloudflare DNS 现状要点
- `www` 记录曾指向 Elastic Beanstalk/ALB 域名且为 DNS only。
- `staging` 记录指向某 CloudFront 域名（历史遗留测试）。
- Apex `cmfinancial.com` 通过 Cloudflare CNAME flattening 指向新 CloudFront 域名是可行且推荐做法；保持 DNS only。

## AWS 成本优化排查结论（基于账单与控制台截图）
- 账单主要项：EC2、EBS、RDS、ELB、VPC Public IPv4、Lightsail、WAF、Data Transfer、Secrets Manager、CloudWatch。
- 已确认可节省项：
  - Lightsail 存在未挂载的静态 IP：`sip01`（Tokyo，Not Attached），建议删除以消除不必要费用。
- EBS：新加坡区查看到 2 个卷均为 In-use（无孤儿卷），无需处理。
- Elastic IP：新加坡区存在 2 个 EIP 且均已关联（非孤儿），需进一步确认是否确有必要（可能与实例公网访问/NAT 架构相关）。

## 产出
- 已在群内输出《官网安全架构升级与 SFC 合规总结报告》草案（CloudFront + WAF + 强制 HTTPS + Shield Standard；遗留 www 域名占用与 DNS 切换事项）。
