## 2026-03-23

- CMF-Bookkeeping 项目交接：已从 GitHub clone `davidluo202/CMF-Bookkeeping`，阅读 `DESIGN.md` / `ROADMAP.md`，确认技术栈为 Python/FastAPI + React 前端。
- 代码审计结果：v0.3 Trial Balance、v0.4 FRR Balance Sheet 已完成；v0.5/0.6/0.7 未完全实现（前端损益表/费用明细为 Coming Soon 占位）。
- 已实现并推送：`v0.7.260323.001`（commit `fe98de6`）：
  - 后端：新增 `GET /reports/income-statement`（按月+YTD 损益表）、`GET /reports/expense-detail`（费用明细）、`POST /period/month-end-close`（月末结账锁定）、`GET /period/closed-months`。
  - 前端：ReportCenter 实现损益表与费用明细两个 Tab；PeriodManagement 增加月末结账 UI（含二次确认、已结账历史）。
  - `main.py` 里 `MILESTONE` bump 到 0.7。
- Mission Control 部署网络信息：NAS Ubuntu VM `ens3` 局域网 IP 为 `192.168.1.247`；Tailscale IP 为 `100.107.235.107`。建议：仅局域网访问用 192.168.1.247；跨网访问优先用 Tailscale 100.107.235.107。
